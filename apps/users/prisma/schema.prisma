generator client {
  provider        = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./db.sql"
}

model User {
  id       Int      @id @default(autoincrement())
  // Add other fields for the user model here (e.g., name, email)
}
